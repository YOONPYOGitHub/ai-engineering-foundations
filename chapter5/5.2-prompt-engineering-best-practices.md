# 5.2 Prompt Engineering Best Practices

## 📚 개요

효과적인 프롬프트 작성을 위한 검증된 모범 사례를 다룹니다. OpenAI, Anthropic, Meta, Google의 공식 가이드와 실제 배포 경험에서 도출된 기법들입니다.

---

## 📝 Best Practices 개요

```
┌─────────────────────────────────────────────────────────┐
│           Prompt Engineering Best Practices              │
├─────────────────────────────────────────────────────────┤
│  1. Write Clear and Explicit Instructions               │
│  2. Provide Sufficient Context                          │
│  3. Break Complex Tasks into Simpler Subtasks           │
│  4. Give the Model Time to Think (CoT)                  │
│  5. Iterate on Your Prompts                             │
│  6. Evaluate Prompt Engineering Tools                   │
│  7. Organize and Version Prompts                        │
└─────────────────────────────────────────────────────────┘
```

---

## 1️⃣ Write Clear and Explicit Instructions

### 모호함 없이 설명하기

```
❌ 나쁜 예:
"이 에세이에 점수를 매겨줘"

✅ 좋은 예:
"이 에세이에 1-5점 척도로 점수를 매겨줘.
- 정수만 사용 (소수점 없음)
- 불확실하면 'Uncertain' 출력
- 점수 선택 이유를 한 문장으로 설명"
```

### 페르소나 부여하기

페르소나는 모델이 올바른 관점에서 응답하도록 돕습니다.

| 페르소나 | 에세이 평가 결과 |
|----------|------------------|
| (없음) | "I like chickens..." → 2/5 |
| 1학년 교사 | "I like chickens..." → 4/5 |

```python
# 페르소나 예시
system_prompt = """
You are an experienced first-grade teacher who encourages 
young students and celebrates their creative expression. 
Evaluate this essay with age-appropriate expectations.
"""
```

### 예시 제공하기

예시는 모호함을 줄이고 원하는 응답 형식을 명확히 합니다.

```
# 예시 없이
Q: Will Santa bring me presents on Christmas?
A: Santa Claus is a fictional character... (원치 않는 응답)

# 예시 포함
Q: Is the tooth fairy real?
A: Of course! Put your tooth under your pillow tonight...

Q: Will Santa bring me presents on Christmas?
A: Yes, absolutely! Santa loves to bring presents... (원하는 응답)
```

### 토큰 효율적인 예시 형식

| 형식 | 토큰 수 (GPT-4) |
|------|----------------|
| `Input: chickpea\nOutput: edible` | 38 |
| `chickpea --> edible` | 27 |

> **팁**: 성능이 동일하다면 더 짧은 형식 선택

### 출력 형식 명시하기

```
지시사항에 포함할 것:
- 원하는 길이 (간결하게, 3문장 이내 등)
- 형식 (JSON, Markdown, 표 등)
- 피해야 할 것 (서론 없이 바로 답변 등)
```

**마커 사용하기:**

```
❌ 마커 없음:
Label as edible or inedible.
chicken

→ "tacos --> edible" (입력에 계속 추가)

✅ 마커 있음:
Label as edible or inedible.
chicken -->

→ "edible" (올바른 출력)
```

---

## 2️⃣ Provide Sufficient Context

### 컨텍스트의 중요성

> 충분한 컨텍스트 = 더 좋은 응답 + 환각 감소

```
컨텍스트 없음
    ↓
모델이 내부 지식에 의존
    ↓
환각 가능성 증가
```

### 컨텍스트 구성 방법

| 방법 | 설명 |
|------|------|
| **직접 제공** | 프롬프트에 문서/데이터 포함 |
| **RAG** | 데이터베이스에서 관련 정보 검색 |
| **Web Search** | 실시간 웹 검색 결과 포함 |
| **Tool Use** | 도구를 통해 정보 수집 |

### 모델 지식을 컨텍스트로 제한하기

```
시나리오: Skyrim 게임 캐릭터 역할극

문제: "스타벅스에서 뭐 좋아해요?" 같은 질문에 답변하면 안 됨

해결책:
1. "제공된 컨텍스트만 사용하여 답변하세요" 명시
2. 답변의 출처를 인용하도록 요청
3. 답변할 수 없는 질문 예시 제공
```

> **주의**: 프롬프팅만으로는 100% 보장 불가능. 파인튜닝이나 전용 학습이 필요할 수 있음.

---

## 3️⃣ Break Complex Tasks into Simpler Subtasks

### 태스크 분해의 이점

```
복잡한 단일 프롬프트
        ↓
┌──────────────────────────────────────┐
│ 문제점:                              │
│ - 디버깅 어려움                       │
│ - 모니터링 어려움                     │
│ - 에러 전파                           │
└──────────────────────────────────────┘

분해된 프롬프트 체인
        ↓
┌──────────────────────────────────────┐
│ 장점:                                │
│ - 각 단계 모니터링 가능               │
│ - 특정 단계만 디버깅                  │
│ - 병렬 실행 가능                      │
│ - 단계별 다른 모델 사용 가능          │
└──────────────────────────────────────┘
```

### 예시: 고객 지원 챗봇

**기존 (단일 프롬프트):**
```
1,500+ 토큰의 거대한 프롬프트
    ↓
복잡, 디버깅 어려움, 비용 높음
```

**분해 후:**
```
[Prompt 1: Intent Classification]
사용자 요청 → Primary/Secondary 카테고리 분류
                    ↓
[Prompt 2: Response Generation]
카테고리별 전문화된 응답 생성
```

**Intent Classification 프롬프트:**
```
You will be provided with customer service queries. 
Classify each query into a primary category and a secondary category.
Provide your output in JSON format with keys: primary and secondary.

Primary categories: Billing, Technical Support, Account Management, General Inquiry

Technical Support secondary categories:
- Troubleshooting
- Device compatibility
- Software updates
```

**Troubleshooting 응답 프롬프트:**
```
You will be provided with customer service inquiries that require 
troubleshooting in a technical support context. Help the user by:
- Ask them to check that all cables are connected
- If issue persists, ask which router model they are using
- If still unresolved, connect them to IT support: {"IT support requested"}
```

### 병렬화

```python
# 독립적인 태스크는 병렬 실행
async def generate_stories(topic):
    tasks = [
        generate_story(topic, level="1st grade"),
        generate_story(topic, level="8th grade"),
        generate_story(topic, level="college")
    ]
    return await asyncio.gather(*tasks)
```

### 비용 분석

| 접근법 | 장점 | 단점 |
|--------|------|------|
| 단일 프롬프트 | 단순, 낮은 지연시간 | 디버깅 어려움 |
| 분해된 프롬프트 | 모니터링/디버깅 용이 | 더 많은 API 호출 |

> **GoDaddy 사례**: 1,500 토큰 프롬프트를 분해 → 성능 향상 + 비용 절감

---

## 4️⃣ Give the Model Time to Think

### Chain-of-Thought (CoT)

> **CoT**: 모델에게 단계별로 생각하도록 요청하여 추론 능력 향상

Wei et al., 2022에서 소개 (ChatGPT 출시 약 1년 전)

### CoT 변형들

| 유형 | 프롬프트 | 예시 |
|------|----------|------|
| **Zero-shot CoT** | "Think step by step" 추가 | "어떤 동물이 더 빠른가? 단계별로 생각해." |
| **Zero-shot CoT** | "Explain your rationale" 추가 | "답변 전에 이유를 설명해." |
| **Structured CoT** | 단계 명시 | "1. 가장 빠른 개 품종 속도 확인\n2. 가장 빠른 고양이 품종 속도 확인\n3. 비교" |
| **One-shot CoT** | 추론 예시 포함 | 상어 vs 돌고래 예시 후 고양이 vs 개 질문 |

### CoT 예시

```
# Zero-shot CoT
Q: Which animal is faster: cats or dogs? 
   Think step by step before arriving at an answer.

A: Let me think through this step by step:
1. The fastest dog breed is the Greyhound, reaching ~72 km/h
2. The fastest cat is the Egyptian Mau, reaching ~48 km/h
3. Therefore, dogs are faster than cats.
```

### Self-Critique (자기 비판)

```
단계:
1. 모델이 답변 생성
2. 모델에게 자신의 답변 검토 요청
3. 오류 발견 시 수정된 답변 생성
```

```python
# Self-critique 예시
initial_answer = model.generate(question)
critique_prompt = f"""
Review this answer for accuracy and completeness:
{initial_answer}

If you find any errors, provide a corrected answer.
"""
final_answer = model.generate(critique_prompt)
```

### CoT의 Trade-off

| 장점 | 단점 |
|------|------|
| 추론 능력 향상 | 지연시간 증가 |
| 환각 감소 (LinkedIn 발견) | 토큰 비용 증가 |
| 디버깅 가능 | 예측 불가능한 단계 수 |

---

## 5️⃣ Iterate on Your Prompts

### 반복 개선 프로세스

```
┌─────────────┐
│ 초기 프롬프트 │
└──────┬──────┘
       ↓
┌─────────────┐
│ 테스트      │
└──────┬──────┘
       ↓
┌─────────────┐
│ 결과 분석   │
└──────┬──────┘
       ↓
  개선 필요? ──Yes──→ 프롬프트 수정 ───┐
       │                             │
       No                            │
       ↓                             │
┌─────────────┐                      │
│   완료      │←─────────────────────┘
└─────────────┘
```

### 반복 개선 체크리스트

- [ ] 다양한 프롬프트 구조 실험
- [ ] 프롬프트 버전 관리
- [ ] 일관된 평가 메트릭 사용
- [ ] 평가 데이터 표준화
- [ ] 전체 시스템 맥락에서 평가
- [ ] 모델별 가이드 참조

---

## 6️⃣ Evaluate Prompt Engineering Tools

### 자동화 도구 유형

| 도구 | 기능 |
|------|------|
| **DSPy** | 프롬프트 최적화 자동화 |
| **Promptbreeder** | 진화적 전략으로 프롬프트 개선 |
| **TextGrad** | AI 기반 프롬프트 최적화 |
| **Guidance/Outlines** | 구조화된 출력 가이드 |

### AI로 프롬프트 작성하기

```python
meta_prompt = """
Help me write a concise prompt for an application that 
grades college essays between 1 and 5.

Requirements:
- Clear scoring criteria
- Consistent output format
- Examples of edge cases
"""

generated_prompt = model.generate(meta_prompt)
```

### ⚠️ 도구 사용 시 주의사항

1. **숨겨진 API 호출**: 프롬프트 변형 + 평가 = 많은 API 호출
   ```
   30개 평가 예시 × 10개 프롬프트 변형 = 300 API 호출
   ```

2. **도구 개발자의 실수**: 잘못된 템플릿, 오타 등
   
3. **예고 없는 변경**: 도구 업데이트로 인한 동작 변경

> **원칙**: "Show Me the Prompt" - 항상 도구가 생성하는 프롬프트 검사

---

## 7️⃣ Organize and Version Prompts

### 프롬프트와 코드 분리

```python
# prompts.py
GPT4o_ENTITY_EXTRACTION_PROMPT = """
Extract named entities from the following text.
Output as JSON with keys: person, organization, location.
"""

# application.py
from prompts import GPT4o_ENTITY_EXTRACTION_PROMPT

def query_openai(user_prompt):
    completion = client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {"role": "system", "content": GPT4o_ENTITY_EXTRACTION_PROMPT},
            {"role": "user", "content": user_prompt}
        ]
    )
```

### 분리의 장점

| 장점 | 설명 |
|------|------|
| **재사용성** | 여러 애플리케이션에서 동일 프롬프트 사용 |
| **테스팅** | 코드와 프롬프트 독립적 테스트 |
| **가독성** | 코드와 프롬프트 분리로 명확함 |
| **협업** | SME가 코드 없이 프롬프트 작업 가능 |

### 프롬프트 메타데이터

```python
from pydantic import BaseModel
from datetime import datetime

class Prompt(BaseModel):
    model_name: str
    date_created: datetime
    prompt_text: str
    application: str
    creator: str
    version: str
    sampling_params: dict  # temperature, top_p 등
    input_schema: dict
    output_schema: dict
```

### Prompt Catalog

```
버전 관리 옵션:

1. Git과 함께 버전 관리
   장점: 단순
   단점: 모든 앱이 동일 버전 강제

2. 별도 Prompt Catalog
   장점: 앱별 다른 버전 사용 가능
   장점: 검색, 의존성 추적
```

---

## 💡 핵심 요약

| Best Practice | 핵심 |
|---------------|------|
| Clear Instructions | 모호함 제거, 페르소나, 예시, 형식 명시 |
| Sufficient Context | 환각 감소, RAG/도구 활용 |
| Task Decomposition | 복잡한 작업을 작은 단계로 분해 |
| Chain-of-Thought | "단계별로 생각해" → 추론 능력 향상 |
| Iterate | 체계적 실험, 버전 관리, 평가 |
| Tools | 검사하고 비용 모니터링 |
| Organization | 코드와 프롬프트 분리, 메타데이터 |

---

## ❓ 토론 질문

1. CoT가 모든 작업에 효과적일까요? 어떤 경우에 역효과가 날 수 있을까요?

2. 프롬프트 버전 관리에서 A/B 테스트를 어떻게 구현할 수 있을까요?

3. 자동화된 프롬프트 최적화 도구의 한계는 무엇일까요?
