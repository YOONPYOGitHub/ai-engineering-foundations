# 1.1 The Rise of AI Engineering

> **"Scale이 모든 것을 바꿨다"**  
> AI post-2020을 한 단어로: Scale

[← 목차로 돌아가기](./README.md)

---

## 📋 목차

- [Language Models의 역사](#language-models의-역사)
- [Self-Supervision: 게임 체인저](#self-supervision-게임-체인저)
- [Transformer의 혁명](#transformer의-혁명)
- [Scaling Laws와 Emergent Abilities](#scaling-laws와-emergent-abilities)
- [Multimodal로의 확장](#multimodal로의-확장)
- [Foundation Models의 정의](#foundation-models의-정의)
- [AI Engineering의 탄생](#ai-engineering의-탄생)
- [핵심 요약](#핵심-요약)

---

## Language Models의 역사

### 기본 개념

**Language Model이란?**
> 하나 이상의 언어에 대한 통계적 정보를 인코딩하는 모델

직관적으로, 주어진 문맥에서 특정 단어가 나타날 확률을 알려줍니다.

**예시:**
```
문맥: "My favorite color is __"
Language Model 예측:
- "blue" (높은 확률) ✓
- "car" (낮은 확률) ✗
```

### 역사적 배경

**1905년: Sherlock Holmes**
- "The Adventure of the Dancing Men" 이야기
- 영어에서 가장 흔한 글자는 E
- 가장 흔한 막대기 그림 = E
- 단순한 통계적 정보 활용

**1951년: Claude Shannon**
- 2차 세계대전 중 적의 메시지 해독
- "Prediction and Entropy of Printed English" 논문
- Entropy 개념 도입 (현재도 사용됨)

### Token: Language Model의 기본 단위

**Token이란?**
- 문자(character), 단어(word), 또는 단어의 일부
- 모델에 따라 다름

**GPT-4 예시:**
```
"I can't wait to build AI applications"
↓
["I", " can", "'t", " wait", " to", " build", " AI", " applications"]
9 tokens

참고: "can't" → ["can", "'t"] (2 tokens)
```

**Tokenization**
- 원본 텍스트를 토큰으로 나누는 과정
- GPT-4: 평균 1 token ≈ 0.75 words
- 100 tokens ≈ 75 words

**Vocabulary**
- 모델이 사용할 수 있는 모든 토큰의 집합
- Mixtral 8x7B: 32,000 tokens
- GPT-4: 100,256 tokens

**왜 Token을 사용하는가?**

1. **의미있는 구성 요소**
   ```
   "cooking" → ["cook", "ing"]
   둘 다 의미 보존
   ```

2. **효율성**
   ```
   Character < Token < Word
   
   Token이 최적:
   - Word보다 적은 vocabulary
   - Character보다 많은 의미
   ```

3. **미지의 단어 처리**
   ```
   "chatgpting" → ["chatgpt", "ing"]
   구조 이해 가능
   ```

### Language Model의 두 가지 타입

**1. Masked Language Model**

특징:
- 문장 어디서든 빠진 토큰 예측
- 앞뒤 문맥 모두 사용 (bidirectional)
- Fill in the blank 태스크

예시:
```
"My favorite __ is blue"
→ "color" 예측
```

대표 모델:
- **BERT** (Bidirectional Encoder Representations from Transformers)

사용 사례:
- 감성 분석
- 텍스트 분류
- 코드 디버깅 (앞뒤 코드 이해 필요)

**2. Autoregressive Language Model**

특징:
- 다음 토큰 예측
- 이전 토큰만 사용
- 계속해서 토큰 생성 가능

예시:
```
"My favorite color is __"
→ "blue" 예측
→ 계속 생성: "blue and I"
```

현재:
- **텍스트 생성의 표준**
- 이 책에서 "language model" = autoregressive model

비교:
```
Masked LM:  [context] __ [context] → token
Autoregressive: [previous tokens] → next token
```

**Generative AI**

Language model의 출력:
- 고정된 vocabulary
- 무한한 가능한 출력
- **Open-ended**: 열린 결과

→ 이것이 "Generative AI"

### Completion Machine

**핵심 개념:**
> Language Model = Completion Machine  
> 주어진 텍스트(prompt)를 완성하려고 시도

**예시:**
```
Prompt: "To be or not to be"
Completion: ", that is the question."
```

**중요:**
- Completion = Prediction (확률 기반)
- 정답 보장 ✗
- Probabilistic nature → 흥미롭고도 좌절스러움

**Completion의 힘**

많은 태스크를 completion으로 프레이밍 가능:
- 번역
- 요약
- 코딩
- 수학 문제 풀이

**번역 예시:**
```
Prompt: "How are you in French is …"
Completion: "Comment ça va"
```

**분류 예시:**
```
Prompt: 
"Question: Is this email likely spam? 
Here's the email: <email content>
Answer:"

Completion: "Likely spam"
→ Spam classifier!
```

**한계:**
- Completion ≠ Conversation
- 질문에 질문으로 대답할 수도 있음
- Post-training 필요 (Chapter 2에서 다룸)

---

## Self-Supervision: 게임 체인저

### 기존 방식: Supervised Learning

**프로세스:**
```
1. 데이터 수집
2. 사람이 레이블링
3. 모델 학습
4. 새 데이터에 적용
```

**예시: Fraud Detection**
```
Input: Transaction
Label: "Fraud" or "Not Fraud"
→ 모델 학습
→ 새 거래 예측
```

**2010년대 성공: ImageNet + AlexNet**
- 1백만 이미지
- 1,000 카테고리 ("car", "balloon", "monkey")
- Deep Learning 혁명의 시작

### Supervision의 문제점

**1. 비용**
```
이미지 1개 레이블: $0.05
1백만 이미지: $50,000

2명이 cross-check: $100,000
1백만 카테고리로 확장: $50,000,000!
```

**2. 복잡도**
```
일상 물체 레이블: 누구나 가능, 저렴
Latin 번역: 더 비쌈
CT 스캔 암 진단: 천문학적 비용
```

**3. 확장성**
```
새 카테고리 추가 → 새 레이블 필요
새 태스크 → 처음부터 다시
```

### Self-Supervision: 혁명

**핵심 아이디어:**
> 데이터가 스스로 레이블 제공

**Language Modeling 예시:**

문장: "I love street food."

생성되는 학습 샘플:
```
Input (context)              → Output (next token)
<BOS>                        → I
<BOS>, I                     → love
<BOS>, I, love               → street
<BOS>, I, love, street       → food
<BOS>, I, love, street, food → .
<BOS>, ..., food, .          → <EOS>

6개의 학습 샘플!
레이블링 비용: $0
```

**Special Tokens:**
- `<BOS>`: Beginning of Sequence
- `<EOS>`: End of Sequence (언제 멈출지 알려줌)

**수학적 표현:**
```
Supervised:
P(label | input) ← 명시적 레이블 필요

Self-supervised:
P(next_word | previous_words) ← 데이터가 레이블
```

**Self-supervised vs Unsupervised:**
- Self-supervised: 레이블을 데이터에서 추론
- Unsupervised: 레이블 전혀 불필요

### 왜 혁명적인가?

**1. 무한한 데이터**
```
레이블된 데이터: 수백만 ~ 수억 개
인터넷 텍스트: 수조 개 토큰

기존: "데이터가 부족해요" 😢
Self-supervision: "데이터가 너무 많아요" 🎉
```

**2. 비용 절감**
```
Supervised:
$1/레이블 × 1M 레이블 = $1,000,000

Self-supervised:
$0 × ∞ 데이터 = $0
```

**3. 범용 학습**
```
감성 분석 데이터:
→ 감성 분석만 가능

인터넷 전체 텍스트:
→ 모든 것 학습 가능
```

**4. Transfer Learning**
```
Pre-training (self-supervised):
- 인터넷 전체에서 학습
- 범용 능력 획득

    ↓ (Transfer)

Finetuning (task-specific):
- 적은 데이터로 특화
- 전문 능력 개발
```

**결과:**
> Self-supervision이 Language Models를 Large Language Models로 확장 가능하게 만듦

---

## Transformer의 혁명

### 2017년: "Attention Is All You Need"

**발표:** Google

**핵심 기술:** Attention Mechanism

**장점:**
- 병렬 처리 가능 (RNN과 달리)
- 긴 문맥 처리 가능
- 효율적 학습

**영향:**
```
2017: Transformer 발표
2018: BERT (Masked LM), GPT-1 (Autoregressive LM)
2019: GPT-2
2020: GPT-3
→ Large Language Models 시대 시작
```

### 이전 모델들의 한계

**Word2Vec (2013)**
- 단어를 벡터로 표현
- 예: king - man + woman ≈ queen
- 하지만 문맥 이해 제한적

**RNN/LSTM**
- 순차적 정보 처리
- 긴 의존성 학습 가능
- 하지만 병렬 처리 불가, 여전히 한계

**N-gram 모델 (1990s-2000s)**
- 통계적 방법
- 규칙 기반
- 긴 문맥 불가, 복잡한 패턴 학습 어려움

---

## Scaling Laws와 Emergent Abilities

### 모델 크기: Parameters

**Parameter란?**
> 학습 과정에서 업데이트되는 모델 내부 변수  
> 일반적으로 parameters ↑ → 학습 능력 ↑

**"Large"의 변화:**
```
2018년 GPT-1:   117M parameters → "large"
2019년 GPT-2:   1.5B parameters → 117M은 이제 "small"
2020년 GPT-3:   175B parameters → 1.5B도 "small"
2024년 현재:    100B+ → "large"
미래:           ???
```

**성장 비율:**
```
GPT-1 → GPT-2:  13배
GPT-1 → GPT-3:  1,500배
GPT-1 → GPT-4:  14,500배 (추정)
```

### Scaling Laws의 발견

**3가지 Scale:**
1. **Model Size** (Parameters)
2. **Data Size** (Tokens)  
3. **Compute** (FLOPs)

**핵심 발견:**
```
더 큰 모델 + 더 많은 데이터 + 더 많은 계산
= 더 나은 성능 (예측 가능하게 Predictable)
```

**AlexNet 논문 (2012)에서 이미:**
> "우리의 모든 실험은 더 빠른 GPU와 더 큰 데이터셋이 나오기만을 기다리면 결과가 개선될 것임을 시사한다"

→ Ilya Sutskever (AlexNet 저자) → OpenAI 공동 창립 → GPT로 현실화

**왜 큰 모델이 더 많은 데이터 필요?**
```
큰 모델 = 더 큰 학습 능력
→ 성능 최대화하려면 더 많은 데이터 필요

작은 데이터로 큰 모델 학습 가능하지만:
→ 낭비 (작은 모델로도 비슷한 성능)
```

### Emergent Abilities (창발적 능력)

**정의:**
> 모델이 특정 크기를 넘으면 갑자기 나타나는 새로운 능력

**1. Few-Shot Learning**

Small Model:
```
Q: "Translate to French: Hello"
A: "Bonjour"
Q: "Translate to French: Goodbye"
A: [실패] ✗
```

Large Model (GPT-3):
```
Q: "Translate to French: Hello"
A: "Bonjour"
Q: "Translate to French: Goodbye"  
A: "Au revoir" ✓
Q: "Translate to French: How are you?"
A: "Comment allez-vous?" ✓

→ 예시만 보고 패턴 학습!
```

**2. Chain-of-Thought Reasoning**

Small Model:
```
Q: "What is 15% of 80?"
A: "12" (답만)
```

Large Model:
```
Q: "What is 15% of 80?"
A: "Let me think step by step:
    - 15% = 0.15
    - 0.15 × 80 = 12
    Therefore, 15% of 80 is 12."
    
→ 추론 과정까지 표현!
```

**3. Instruction Following**

Small Model:
```
Input: "Write a poem about AI"
Output: [임의의 텍스트 생성]
```

Large Model:
```
Input: "Write a poem about AI"
Output: [실제로 AI에 대한 시 작성]

→ 지시사항을 이해하고 따름!
```

### Phase Transitions (임계점)

**현상:**
```
모델 크기: 0 → 1B → 10B → 100B → 1T

성능:
         ───────────────────┐
                            │  ← 갑자기 점프!
                            │
         ─────────────────── 

특정 크기를 넘으면 급격한 성능 향상
```

**시사점:**
- "충분히 크면" 새로운 능력 출현
- 예측하기 어려움
- 더 크면 또 무슨 능력이?

**Chip Huyen의 경험:**
> "GPT-3가 놀라운 것은 크기나 능력이 아니었다.  
> 작은 품질 향상이 엄청난 수의 애플리케이션을 가능하게 했다는 것이 놀라웠다."

---

## Multimodal로의 확장

### 텍스트를 넘어서

**인간의 인식:**
- 언어뿐만 아니라
- 시각, 청각, 촉각 등

**AI의 필요:**
> 실제 세계에서 작동하려면 텍스트 이상 필요

**진화:**
```
Language Models (2020):
Input:  텍스트
Output: 텍스트

Large Multimodal Models (2021~):
Input:  텍스트 + 이미지 + 오디오 + 비디오
Output: 텍스트 + 이미지 + 오디오 + 비디오
```

### Multimodal Model의 작동

**Language Model:**
```
다음 토큰 예측:
← 텍스트 토큰만 사용
```

**Multimodal Model:**
```
다음 토큰 예측:
← 텍스트 + 이미지 + ... 토큰 사용
```

### CLIP: Vision + Language (2021)

**개발:** OpenAI

**학습 방법:** Natural Language Supervision
- Self-supervision의 변형
- (이미지, 텍스트) 쌍이 인터넷에 함께 나타남
- 4억 개의 (이미지, 텍스트) 쌍
- ImageNet의 400배 크기
- 레이블링 비용 제로

**능력:**
- Zero-shot image classification
- Image-text similarity

**예시:**
```
Query: "a photo of a dog"
→ CLIP이 개 사진들을 찾아냄
(학습 중에 "개" 카테고리 명시적 학습 없이!)
```

**중요:**
- CLIP은 생성 모델 아님
- Embedding 모델 (Chapter 3에서 자세히)
- Multimodal 생성 모델의 백본

### 주요 Multimodal Models

**Vision + Language:**
- **Flamingo** (DeepMind, 2022): 이미지 + 질문 → 답변
- **LLaVA**: 오픈소스 멀티모달
- **GPT-4V**: GPT-4 + Vision
- **Claude 3**: 이미지 + 텍스트 이해
- **Gemini**: 다양한 modality

**Text → Image:**
- **DALL-E 3**: 텍스트 설명 → 이미지
  ```
  "An astronaut riding a horse on Mars"
  → [해당 이미지 생성]
  ```
- **Midjourney**: 예술적 이미지
- **Stable Diffusion**: 오픈소스

**Audio:**
- **Whisper** (OpenAI, 2022):
  - 오디오 → 텍스트 (transcription)
  - 다국어 음성 인식
  - 번역
  - 시끄러운 환경에서도 작동
- **Text-to-Speech**: 텍스트 → 자연스러운 음성

**Video:**
- **Runway Gen-2**: 텍스트 → 4초 비디오
- **Pika Labs**: 텍스트/이미지 → 비디오
- **Sora** (OpenAI, 2024):
  - 텍스트 → 최대 60초 비디오
  - 영화 수준 품질
  - 물리적 일관성
  - 복잡한 장면

---

## Foundation Models의 정의

### Stanford HAI 정의 (2021)

> **Foundation Model:**  
> "대규모 데이터로 학습되어 다양한 downstream 태스크에 적응될 수 있는 모델"

### 핵심 특징

**1. Scale**
```
- 수십억 ~ 수조 parameters
- 수조 토큰의 학습 데이터
- 막대한 계산량
```

**2. Generality (범용성)**
```
- 단일 태스크 아님
- 다양한 도메인
- 여러 modality
```

**3. Adaptability (적응성)**
```
- Prompt engineering
- Few-shot learning
- Finetuning
```

### 주요 Foundation Models

**Text (LLMs):**
- GPT-4 (OpenAI)
- Claude (Anthropic)
- Gemini (Google)
- Llama 3 (Meta)

**Vision:**
- CLIP (OpenAI)
- DALL-E 3 (OpenAI)
- Midjourney
- Stable Diffusion

**Audio:**
- Whisper (OpenAI)
- AudioLM (Google)

**Video:**
- Sora (OpenAI)
- Runway Gen-2

**Multimodal:**
- GPT-4V (Vision)
- Gemini 1.5 (Text, Image, Video, Audio)
- Claude 3

### 왜 "Foundation"인가?

**Foundation = 기초, 토대**

전통적 AI:
```
App 1 → Model 1
App 2 → Model 2
App 3 → Model 3

각 애플리케이션마다 별도 모델
```

Foundation Models:
```
         ┌→ App 1
         │
Foundation ┼→ App 2
  Model   │
         └→ App 3

하나의 모델 → 여러 애플리케이션
```

**건축 비유:**
```
건축:
Foundation (기초) → 그 위에 다양한 건물

AI:
Foundation Model → 그 위에 다양한 앱
```

### Task-Specific → General-Purpose

**이전:**
- 감성 분석 모델 → 감성 분석만
- 번역 모델 → 번역만

**Foundation Models:**
- Out-of-the-box로 여러 태스크 가능
- Sentiment analysis + Translation + Coding + ...

**하지만:**
- 특정 태스크에 최적화 가능
- Prompt engineering, RAG, Finetuning

**예시: 제품 설명 생성**

Out-of-the-box:
```
✓ 정확한 설명 생성
✗ 브랜드 voice 누락
✗ 핵심 메시지 놓침
✗ 마케팅 cliche 많음
```

적응 후:
```
✓ 브랜드 voice
✓ 핵심 메시지
✓ 고품질
```

적응 방법:
- **Prompt Engineering**: 상세한 지시 + 예시
- **RAG**: 고객 리뷰 데이터베이스 연결
- **Finetuning**: 고품질 제품 설명으로 추가 학습

---

## AI Engineering의 탄생

### AI Engineering이란?

**정의:**
> Foundation Models 위에 애플리케이션을 구축하는 과정

**차이:**
```
전통적 ML Engineering:
- ML 모델 개발

AI Engineering:
- 기존 Foundation Models 활용
```

### 왜 지금인가?

**3가지 요인이 함께 작용:**

**Factor 1: General-Purpose AI 능력**

이전:
```
특정 태스크만 가능
새 애플리케이션 = 새 모델 필요
```

현재:
```
거의 모든 태스크 가능
이전에 불가능했던 것도 가능
미래에는 더 많은 것 가능

→ 수요 폭증
```

예시:
- AI가 인간처럼 글 쓰기 → 모든 커뮤니케이션 자동화
- 즉시 고품질 이미지/비디오 생성 → 창작 민주화
- 학습 데이터 합성, 알고리즘 개발, 코드 작성 → AI가 AI 만들기

**Factor 2: 증가한 투자**

ChatGPT 성공 → 투자 급증

Goldman Sachs 추정:
```
2025년 AI 투자:
- 미국: $100B
- 글로벌: $200B

참고: 미국 공교육 예산 $900B의 1/9
```

S&P 500 기업:
```
2022 Q2: 11% AI 언급
2023 Q2: 33% AI 언급 (3배)

AI 언급 기업 주가: +4.6%
일반 기업 주가: +2.4%
```

Matt Ross (Scribd):
> "2022년 4월 → 2023년 4월  
> AI 비용 추정치가 2 orders of magnitude 감소 (100배)"

**Factor 3: 낮은 진입 장벽**

이전:
```
모델 사용:
- 인프라 필요
- 호스팅 필요
- 전문가 팀 필요
- 수백만 달러
```

현재:
```
Model as a Service:
- API 호출만
- 인프라 불필요
- 즉시 사용 가능
- 저렴한 비용
```

AI가 코드 작성:
```
- 코딩 경험 없어도 앱 개발
- Plain English로 작업
- 누구나 AI 애플리케이션 개발 가능
```

### 가장 빠르게 성장하는 분야

**Sam Altman (OpenAI CEO, 2022년 9월):**
> "대다수 사람들에게 가장 큰 기회는  
> 이 모델들을 특정 애플리케이션에 적응시키는 것"

**GitHub Stars 성장:**
```
2년 내에 AI Engineering 도구들:
- AutoGPT, Stable Diffusion WebUI
- LangChain, Ollama

→ Bitcoin 초과
→ React, Vue 추격 중

이전 어떤 소프트웨어 도구보다 빠른 성장
```

**LinkedIn 조사 (2023년 8월):**
- "Generative AI", "ChatGPT", "Prompt Engineering" 프로필 추가
- 매월 평균 75% 증가

**ComputerWorld:**
> "AI에게 행동을 가르치는 것이  
> 가장 빠르게 성장하는 커리어 스킬"

### 왜 "AI Engineering"이라는 용어?

**고려한 용어들:**
- ML Engineering
- MLOps
- AIOps
- LLMOps

**ML Engineering 선택 안 함:**
- Foundation Models ≠ 전통적 ML
- 중요한 차이점 존재
- ML Engineering = 둘을 포괄하는 용어로는 좋음

**~Ops 선택 안 함:**
- 운영뿐만 아니라 엔지니어링
- 초점은 모델 적응(engineering)

**AI Engineering 선택:**
- 20명 설문: 대부분 선호
- 커뮤니티 선택 존중

---

## 핵심 요약

### Language Models → LLMs

**Self-Supervision의 혁명:**
```
레이블 없이 학습 → 무한한 데이터
비용 제로 → 대규모 모델 가능
```

**Scaling Laws:**
```
더 크면 더 좋음 (예측 가능)
GPT-1 (117M) → GPT-4 (1.7T): 14,500배
```

**Emergent Abilities:**
```
임계점 넘으면 새 능력:
- Few-shot learning
- Chain-of-thought
- Instruction following
```

### LLMs → Foundation Models

**Multimodal 확장:**
```
텍스트 only → 텍스트 + 이미지 + 오디오 + 비디오
CLIP, DALL-E, Whisper, Sora
```

**Generality:**
```
Task-specific → General-purpose
하나의 모델로 여러 태스크
```

**Adaptability:**
```
다양한 방식으로 적응:
- Prompt engineering
- RAG
- Finetuning
```

### Foundation Models → AI Engineering

**3가지 요인:**
```
1. 강력한 범용 능력 → 더 많은 애플리케이션
2. 낮은 진입 장벽 → 누구나 개발 가능
3. 폭발적 투자 → $200B by 2025
```

**결과:**
```
가장 빠르게 성장하는 엔지니어링 분야
Build → Integrate
Code → Prompt
Train → Adapt
```

### Timeline

```
1950s:  첫 Language Model (Shannon)
2013:   Word2Vec
2017:   Transformer ← 전환점
2018:   BERT, GPT-1 (117M)
2019:   GPT-2 (1.5B)
2020:   GPT-3 (175B), Few-shot
2021:   CLIP, "Foundation Models" 용어
2022:   ChatGPT ← AI Engineering 폭발
2023:   GPT-4, Claude, Gemini, Sora
2024:   AI Engineer = 가장 빠른 성장 직군
```

---

**다음**: [1.2 Foundation Model Use Cases →](./1.2-foundation-model-use-cases.md)

[← 목차로 돌아가기](./README.md)
